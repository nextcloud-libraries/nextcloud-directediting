{
  "name": "nextcloud-direct-editing",
  "version": "1.0.0",
  "description": "Nextcloud direct-editing postmessage API",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nextcloud-libraries/nextcloud-directediting.git"
  },
  "scripts": {
    "build": "rollup --config rollup.config.js",
    "dev": "rollup --config rollup.config.js --watch",
    "check-types": "tsc --noEmit",
    "build:doc": "typedoc --out dist/doc lib/index.ts lib/directEditEmit.ts lib/directEditListen.ts --cleanOutputDir false && touch dist/doc/.nojekyll",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nextcloud GmbH and Nextcloud contributors",
  "license": "AGPL-3.0-or-later",
  "bugs": {
    "url": "https://github.com/nextcloud-libraries/nextcloud-directediting/issues"
  },
  "homepage": "https://github.com/nextcloud-libraries/nextcloud-directediting#readme",
  "devDependencies": {
    "@rollup/plugin-replace": "^6.0.2",
    "@rollup/plugin-typescript": "^12.1.2",
    "tslib": "^2.8.1",
    "typedoc": "^0.28.3",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": "^20.0.0",
    "npm": "^9.0.0"
  }
}
